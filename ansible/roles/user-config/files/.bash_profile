# ~/.bash_profile: executed by bash for login shells.
# Sources ~/.bashrc to ensure consistent environment in both login and
# non-login interactive shells.

if [ -f "${HOME}/.bashrc" ]; then
    . "${HOME}/.bashrc"
fi
