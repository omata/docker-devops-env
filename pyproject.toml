[project]
name = "devops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ansible~=7.6",
    "certifi>=2025.1.31",
]

[dependency-groups]
dev = [
    "ruff>=0.9.4",
]
